# Copy this file to .env and fill in your real keys.
# .env is gitignored — never commit real credentials.

# Anthropic (Claude) API — used in Generate_script.py
ANTHROPIC_API_KEY=

# Gemini API — fallback used in Generate_script.py
GEMINI_API_KEY=

# ElevenLabs API — used in Generate_audio.py
ELEVENLABS_API_KEY=
# Optional: specific voice_id (defaults to a generic multilingual voice).
# To find a good one for Portuguese, run client.voices.search(language="pt")
ELEVENLABS_VOICE_ID=

# Gmail SMTP — used in Send_email.py to deliver the finished episode.
# EMAIL_SENDER must be a Gmail address with an "App Password" (requires
# 2-Step Verification): https://myaccount.google.com/apppasswords
EMAIL_SENDER=
EMAIL_APP_PASSWORD=
# Optional: defaults to EMAIL_SENDER if not set
EMAIL_RECIPIENT=
